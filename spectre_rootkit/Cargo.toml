[package]
name = "spectre_rootkit"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
wdk-sys = "0.3.0"

[profile.release]
panic = "abort"
lto = true
opt-level = "z"
strip = true
